{
  "name": "tp-generator-mtsn4jombang-serverless",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for Netlify serverless functions.",
  "scripts": {
    "build": "echo \"No client-side build required, functions are built by Netlify.\""
  },
  "dependencies": {
    "@google/genai": "^1.29.0"
  }
}
